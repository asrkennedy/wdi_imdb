<%= form_for(@character) do |f| %>
  <% if @character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% @character.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <br />
  <div class="field">
    <%= f.label :gender %>
    <%= f.text_field :gender %>
  </div>
  <br />
  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>
  <br />
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>
  <br />
   <div class="field">
    <%= f.label :character_image %>
    <%= f.file_field :character_image %>
  </div>
  <br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
